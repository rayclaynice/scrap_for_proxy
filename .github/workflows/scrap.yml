name: Test Cron



permissions:
  contents: write
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: date
      - run: echo "Hello"
